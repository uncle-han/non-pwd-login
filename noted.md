一个无密码注册登录的系统
1. 我需要做一个注册登录功能，需要你先帮我分析这些功能怎么实现，前端技术栈是 react，后端技术组是 nodejs
2. 注册不需要提供密码，只需要提供邮箱。然后生成一个 二维码，让用户用客户端扫描二维码。二维码包含生成登录数据数的私钥或者公钥 (目前仅支持邮箱，后面有开放支持手机号码)
3. 登录的时候，不需要密码，也不会去服务器拿随机数，而是在本地客户端生成。填入客户端生成的随机数就可以登陆，然后登录成功后，系统下发一个 token 给用户，用于后面的操作
4. 这个登录的随机数生成的方式，你可以参考 RSA token 生成和验证方式
5. 其他应用可以接入这个登录系统


根据上面的要求，你帮我决定以什么方式生成随机数，并用于登录系统。我们先做好规划，目前我们只需要做完成测试用例。通过测试用例还驱动开发

问题1：对于查询随机数客户端，你觉得是在 用原生的安卓/IOS app 还是微信小程序也可以实现？或者网页我的目标是兼容更多用户，减少不必要的 操作。但是也考虑安全性，不会有泄露的风险


dev/uat/prod 环境统一链接到 下面的配置，往后各个环境的配置都不同，确保其独立性

以下是 UAT db的配置
hostname: 192.168.31.123
port: 3306
account：root
pwd: root


本地开发的时候，也可以使用 npm run start:UAT 使用 UAT 的配置

你需要先写测试用例，我 review 过后再写代码



我有一个问题，你现在有没有做一个启动检测？

比如：
1. 项目第一次运行，你需要检查一些 目标数据库有没有 库和表，如果没有，就创建，如果有就忽略
2. 我把 UAT 环境的DB hostname 改成其他，这样在新的 hostname下是没有数据库。你需要运行 一个 init 的文件去 创建数据，创建表
3. 比如我新加一些字段我，启动的时候，检查字段的完整性，没有就新加
4. 比如我删除一些字段我，启动的时候，检查字段的完整性，有就就删除


然我们来开发前端页面
1. 当用户进入注册页面，输入邮箱注册
2. 提交，然后请求服务器，调用 register api 
增加一个功能，当用户在用邮箱注册，将注册 二维码 url 发给注册邮箱，可以点击用需要回调

新增加一个 API：认注册。
1. 当用扫描二维码, 然后自动进行确认注册
2. 或者通过请求/打开 URL，可以是在浏览器，打开之后，自动进行确认注册

现在先想好怎么设计这个请求/打开的URL，和扫描二维码确认注册，然后写测试用例，再生成 API 代码

好像你已经忘记这个项目是做什么的了。
这个是一个无密码登录系统。当登录一个网站的时候，我只需要输入一个随机验证码，这个验证码不是经过服务器请求回来的，而是客户端自己本地生成的了。生成 随机验证码的方式是在确认注册成功之后，会返回一个用于生成 随机数的密钥。
现在的确认注册 API，就是客户在扫描二维码之后，然后自动确注册，会返回 一个密钥用于生成 随机数的


增加一个集成测试：有注册，扫码确认注册，输入验证码登录成功。要给不同的参数进行集成测试


流量一般是从网关服务器请求到这里的， url 是 /ABC-non-pwd-login-uat/v1/auth/register
1. ABC-non-pwd-login-uat 可以替换成其他任意字符。
2. /v1 根据不同的版本有不同的处理方式
3. /auth/register 具体的路由 

根据以上的要求，先生成测试用测试用例，再生成代码
